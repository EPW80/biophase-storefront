# Shopify Configuration
SHOPIFY_STORE_URL=your-store.myshopify.com
SHOPIFY_CLIENT_ID=your_client_id
SHOPIFY_CLIENT_SECRET=your_client_secret

# Storefront API — used for cart/checkout operations (server-side only)
# Either a public (shpua_) or private (shpss_) token works.
# Private tokens must NEVER be prefixed with NEXT_PUBLIC_.
# Omit this var entirely to use tokenless access (cart still works).
SHOPIFY_STOREFRONT_ACCESS_TOKEN=your_storefront_access_token
